<h3>Company list</h3>
<%= link_to 'new', new_company_path %>
  <table>
        <tr>
              <th>Name</th>
              <th>Industry</th>
        </tr>
   <% @companies.each do |company| %>
        <tr>
              <td><%= company.name %></td>
              <td><%= company.industry %></td>
              <td><%= link_to 'Show', company_path(company) %></td>
              <td><%= link_to 'edit', edit_company_path(company) %></td>
              <td><%= link_to 'Delete', company_path(company),
                method: :delete,
                data: {confirm: 'Are yuh sure ?'}
                %></td>

        </tr>
    <% end %>
  </table>

<%= link_to 'Vehicle', vehicles_path %> <br>
<%= link_to 'index', users_path %> <br>
